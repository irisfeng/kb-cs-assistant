# `tysl-local-app-global-v2` Todo List（2026-03-08）

## 目标

把 `tysl-local-app-global-v2` 从“已能稳定联调”继续推进到“知识、提示词、回归口径可复现”，并按当前结论将 `logistics` 标记为“已可内部使用”。

## 当前阶段结论

- `refund / complaint / account / activate` 已进入可持续联调状态。
- `logistics` 已完成真实来源 SOP 导入、首轮异常场景验证，可按“符合官网与知识库文件夹内容范畴即可内部使用”的标准先投入内部使用。
- 前端 UI 主链路 `/api/chat` 已切换并对齐 `tysl-local-app-global-v2`，客服运营后续应以前端 UI 实测结果作为主要验收依据。
- 当前剩余问题主要属于优化项，不再作为内部推进阻塞项。

## P0 已完成

- [x] 回填真实生效提示词
  - 已将 FastGPT 当前实际生效的 `AI 对话` 提示词同步回 `docs/TYSL-LOCAL-APP-GLOBAL-V2-AI-DIALOGUE-PROMPT.md`。
  - 已补齐账号安全保守口径、网络异常先分诊、已拆封退货不得直接泛化结论等约束。

- [x] 同步今天的开发测试记录
  - 已更新 `memory/2026/03/2026-03-08.md`。
  - 已补入物流导入、异常场景验证结果、预览面板异常与当前阶段判断。

- [x] 导入 `logistics` 正式知识
  - 已将 `docs/knowledge-drafts/tysl-logistics-sop-v1.md` 的“建议入库正文”导入 `tysl-local-kb-logistics`。
  - 已完成 collection 建立与分块成功校验。

- [x] 执行物流最小回归
  - 已验证：`已签收未收到`、`下单后一直没发货`、`已拆封后想退货`、`要求赔偿或立即补发`。
  - 当前判断：已满足内部使用标准。

- [x] 对齐前端 UI 全局问答主链路
  - 已确认客服运营主要使用自研前端 UI，而非 FastGPT 编辑页预览。
  - 已将后端全局 `/api/chat` 从旧的 `solution-kb-chat` 工作流切换到 `tysl-local-app-global-v2` 的应用专属 API 通道。
  - 已通过前端 UI 实测：`下单后一直没发货`、`商品已拆封使用后想退货` 两类问法已返回当前正式口径，不再出现旧版虚构流程。

- [x] 补做前端链路典型问法回归
  - 已覆盖 `refund / complaint / activate / logistics` 共 9 条典型客服问法。
  - 当前结论：前端主链路整体可继续内部使用。
  - 唯一需要继续盯住的一致性问题是：`已拆封退货` 在批量回归中仍出现过一次“通常不支持退换货”类变体表述。

## P1 当前优化项

- [x] 对“已拆封退货/退款”场景补一轮负向测试
  - 已验证 4 条变体问法，未再出现“通常不支持退换货”等高风险禁句。
  - 后续如继续优化，重点是压缩少量宽泛表述，而不是阻塞内部使用。

- [x] 复测“下单后一直没发货”场景
  - 已新增 `未发货 / 发货延迟` 独立场景口径，并重新导入物流知识库。
  - 当前回复已可稳定前置“48 小时内安排发货，具体以物流信息为准”的判断。

- [ ] 排查 FastGPT 编辑页“运行预览”卡在 `加载中...`
  - 目标：恢复编辑态右侧预览，方便后续继续手工联调。
  - 说明：真实接口 `/api/v1/chat/completions` 已验证可用，因此这不是知识链路阻塞问题。

- [ ] 评估是否补“功能疑难补充 SOP”
  - 目标：确认是否需要增加一层“功能异常 / 一直不能用 / 操作后仍失败”的全局补充 SOP。

- [ ] 继续压实“已拆封退货”场景一致性
  - 目标：消除前端链路中“通常不支持退换货”一类变体泛化表述，确保该场景稳定保持“先判断是否质量问题/特殊政策，再人工复核”的口径。

## P2 后续再做

- [ ] 评估是否加入 `问题分类` 节点
  - 前提：等核心知识库稳定后，再基于真实回归结果判断。

- [ ] 回到代码侧补 `sessionId` 链路
  - 目标：补齐前后端端到端会话隔离和可追溯性。

- [ ] 统一日志与审计
  - 目标：便于后续追踪知识、提示词、问答结果变更影响。

## 今日收口标准

满足以下条件，即可将 2026-03-08 这轮工作视为阶段收口：

1. 仓库中的提示词文档已与 FastGPT 运行态同步。
2. `logistics` 已完成导入并通过首轮异常场景验证。
3. `logistics` 已达到内部使用标准。
4. 当天开发测试记录已同步到仓库。

## 下一步建议顺序

1. 如继续优化，优先以前端 UI 为主继续补更多真实客服问法回归。
2. 再看是否需要补“功能异常 / 一直不能用”类全局补充 SOP。
3. FastGPT 编辑页预览问题可留作后台运维体验优化，不阻塞客服使用链路。
