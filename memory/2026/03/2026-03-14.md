# 2026-03-14

## 合并前最终检查
- 在 `codex/knowledge-governance` 分支上完成合并前最后一轮检查。
- 本地验证结果：
  - `client` 执行 `npm run build` 通过
  - `server/src/index.js` 语法检查通过
  - `server/src/knowledge-workflow.js` 语法检查通过
  - Excel Q&A 验证脚本结果：`5/5 passed`
  - MinerU 重导验证脚本结果：`23/23 passed`
- 运行链路确认：
  - `http://127.0.0.1:3001/api/solutions` 返回 `200`
  - `http://kb-server.local:3001/api/solutions` 返回 `200`

## 全局 workflow 联调确认
- 在本地 FastGPT 社区版 UI 中确认 `tysl-local-app-global-v2` 已补充产品知识库检索范围：
  - `tysl-local-kb-baichuan`
  - `tysl-local-kb-ebo`
  - `tysl-local-kb-device-shop`
  - `tysl-local-kb-home-ai`
  - `tysl-local-kb-b2b-ict`
- 已执行“保存并发布”，状态显示 `已保存`。
- `/api/chat` 抽测 5 条产品问法：
  - `baichuan`
  - `ebo`
  - `device-shop`
  - `home-ai`
  - `b2b-ict`
  均可返回内容和引用。

## PR 处理
- 发现分支已存在打开中的 PR：
  - [PR #1](https://github.com/irisfeng/kb-cs-assistant/pull/1)
- 由于本机：
  - 未安装 `gh`
  - Playwright 访问 GitHub 时出现 `ERR_CONNECTION_CLOSED`
  改为复用 macOS keychain 中的 GitHub 凭据，通过 GitHub API 完成 PR 查询与更新。
- 已将 PR #1 的标题和描述更新为当前真实成果状态：
  - 标题：`Complete knowledge governance rollout and FastGPT imports`

## PR 合并
- 已通过 GitHub API 合并 PR #1 到 `main`。
- 合并结果：
  - `merged = true`
  - merge commit:
    - `a094314c08aa8b1e322dc52d93392ffe1d8c22bc`
- 当前远端主线：
  - `origin/main` 指向该 merge commit

## 服务状态
- 按用户要求关闭本地后端服务。
- 已停止监听 `3001` 端口的本地 node 进程。
- 验证结果：
  - `http://127.0.0.1:3001/api/solutions` 返回 `connection refused`
- FastGPT Docker 服务未在本轮手动停止，仅关闭了本地项目后端。

## 内测 readiness 评估
- 结论：
  - 具备“小范围上线内测”条件
  - 不具备“直接放量为正式生产客服”条件
- 支撑理由：
  - 产品知识迁移、Q&A 导入、MinerU 图文补传、内部支持脱敏隔离均已完成并验证
  - 全局问答已能命中新产品知识库并返回引用
- 主要风险：
  - FastGPT 默认密码/示例密钥仍需替换
  - 单文档链路仍非真正搜索前硬隔离
  - 回归测试仍以脚本 + 人工烟雾为主，未形成完整自动化基线
  - FastGPT workflow 改动仍依赖 UI 发布，不是完全代码化配置

## 建议下一步
1. 先改 FastGPT 管理员密码、MinIO 密码和示例密钥
2. 准备一份内部测试清单，限定测试范围和人工兜底规则
3. 用户手动重启本地后端后，再做一轮前端页面人工抽问验证

## 今日待办
1. 记录当前内测约束：
   - 当前环境仍为本地 FastGPT 社区版
   - 本轮内测先不改 FastGPT 默认密码、MinIO 密码和示例密钥
   - 仅限本机或可信内网小范围试用，不做公网暴露
2. 产出一份可执行的内部测试清单：
   - 覆盖测试范围、建议问法、问题记录模板、blocker 判定标准、每日收口方式
3. 手动启动本地后端并做启动后验证：
   - `/api/solutions`
   - `/api/chat`
   - `kb-server.local` 访问链路

## 内测准备落地
- 已新增内部试用清单：
  - [INTERNAL-BETA-CHECKLIST-2026-03-14.md](/Users/tony/Documents/GitHub/kb-cs-assistant/docs/INTERNAL-BETA-CHECKLIST-2026-03-14.md)
- 清单内容覆盖：
  - 本轮内测目标与边界
  - 必测高风险题与产品题
  - 负向题设计
  - 问题记录模板
  - blocker 判定标准
  - 每日收口方式
- 本轮明确约束：
  - 由于当前仍是本地 FastGPT 社区版测试环境，本轮不调整 FastGPT 默认密码、MinIO 密码和示例密钥
  - 仅限本机或可信内网小范围内部试用，不做公网暴露

## 服务重启与启动后验证
- 已重新启动本地后端服务：
  - 启动方式：`node src/index.js`
  - 当前日志文件：
    - `/tmp/kb-cs-assistant-server.log`
- 启动后验证：
  - `http://127.0.0.1:3001/api/solutions` 返回 `200`
  - `http://kb-server.local:3001/api/solutions` 返回 `200`
  - 当前 `/api/solutions` 数量仍为 `30`
- 全局问答启动后抽测：
  - 退款分诊问题：可返回结构化答复与引用
  - 物流签收异常问题：可返回结构化答复与引用
- 额外记录：
  - `/api/chat` 当前接口要求使用 `messages` 数组，而不是单个 `message` 字段；本轮已按前端实际请求格式验证通过

## 进入第一轮内部试用的结论
- 当前已满足第一轮内部试用启动条件：
  - 后端服务已启动
  - 全局知识库链路可访问
  - 产品知识与高风险 SOP 检索链路可返回内容和引用
- 仍维持原判断：
  - 适合“小范围内部试用”
  - 仍不建议直接放量为正式生产客服

## 产品库统一命名
- 已新增统一命名表：
  - [PRODUCT-KB-NAMING-CONVENTION-2026-03-14.md](/Users/tony/Documents/GitHub/kb-cs-assistant/docs/PRODUCT-KB-NAMING-CONVENTION-2026-03-14.md)
- 当前统一中文名：
  - `baichuan` -> `视联百川客户端`
  - `ebo` -> `EBO-SE 移动机器人`
  - `device-shop` -> `商城设备类产品`
  - `home-ai` -> `天翼看家 AI 产品`
  - `b2b-ict` -> `政企版 AI 产品`
- 已同步更新内部试用清单中的产品测试项，后续测试、汇报和页面文案建议统一按该表执行。
- 替换边界评估：
  - 当前前端用户可见代码里几乎没有这 5 个英文键名的直接展示
  - 英文键名主要集中在脚本、环境变量、FastGPT 知识库名和历史报告中
  - 结论：不做全仓库字符串替换；页面展示、内测清单、汇报材料使用中文名，技术脚本和历史记录继续保留技术键名

## 前端展示层命名统一
- 已新增前端产品名映射工具：
  - `client/src/utils/productLabels.ts`
- 已接入以下用户可见展示层：
  - `client/src/components/SolutionCard.tsx`
  - `client/src/components/SolutionPreviewDrawer.tsx`
  - `client/src/components/SolutionDetail.tsx`
- 当前展示逻辑：
  - 优先使用 `productName`
  - 其次映射 `productLine`
  - 再从 `title/fileName` 做保守推断
- 验证结果：
  - `client` 执行 `npm run build` 通过
