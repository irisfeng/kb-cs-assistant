# 2026-03-12

- 为全局客服工作流补强了 `activate / acct-sec / logistics(签收异常)` 的知识覆盖。
- 新增 4 篇聚焦草稿并导入 FastGPT：
  - `tysl-activate-bound-by-other-account-focus-v1`
  - `tysl-activate-network-triage-focus-v1`
  - `tysl-acct-sec-abnormal-login-focus-v1`
  - `tysl-logistics-signed-not-received-focus-v1`
- 归档文档：
  - [KNOWLEDGE-COVERAGE-ARCHIVE-2026-03-12.md](/Users/tony/Documents/GitHub/kb-cs-assistant/docs/KNOWLEDGE-COVERAGE-ARCHIVE-2026-03-12.md)
- 结果：
  - `无法绑定`
  - `已被其他账号绑定`
  - `账号疑似被盗/修改绑定信息`
  - `已签收但本人未收到`
  这 4 类问法已从“无引用兜底”提升到“可返回引用的结构化答复”。
- 同时补了通用导入脚本：
  - [import-fastgpt-text.js](/Users/tony/Documents/GitHub/kb-cs-assistant/server/src/scripts/import-fastgpt-text.js)
- 单文档工作流已切换到新的 FastGPT 应用：
  - 应用名：`solution-doc-scope-v1`
  - 后端配置：
    - `FASTGPT_SOLUTION_APP_MODEL=solution-doc-scope-v1`
    - `FASTGPT_SOLUTION_APP_KEY=<solution-doc-scope-v1 专用 key>`
- 单文档最小回归结果：
  - `这份文档主要说明什么内容？`：通过
  - `视联百川客户端适用客户群体是什么？`：通过
  - `退款规则是什么？`：返回“当前文档未找到足够依据，请人工复核。”
  - 同一问题连续追问两次：不再退化成欢迎语，但存在偶发段落重复

## 当前问题
- 单文档 workflow 的 `知识库搜索 -> 搜索过滤` 页面不支持按 `collectionId/sourceName` 做硬过滤。
- 因此，文档外问题即使没有命中当前文档，也可能因为命中了其他文档的 `quoteList` 而先进入 `IF -> AI 对话`。
- 当前“文档外问题返回人工复核”主要依赖 AI 提示词和后端引用过滤，而不是 workflow 的 `ELSE` 分支真实命中。

## 本轮迁移执行
- 已生成正式基线：
  - `tmp/inventory-20260312`
  - `tmp/manifest-20260312`
- 已新增批量导入脚本：
  - `server/src/scripts/import-direct-docx-batch.js`
  - `server/src/scripts/import-derived-markdown-batch.js`
- 已完成 direct docx 导入：
  - 结果：`25 imported + 1 skipped_existing + 0 failed`
- 已完成 Excel 派生 markdown 导入：
  - 结果：`42 imported + 1 skipped_existing + 0 failed`
- 新知识库最终 collection 数：
  - `baichuan = 7`
  - `ebo = 2`
  - `device-shop = 8`
  - `home-ai = 49`
  - `b2b-ict = 3`
- 验证结果：
  - 各知识库抽样 collection 均有 `rawTextLength > 0`
  - 各知识库抽样 collection 均有 `indexAmount > 0`
  - 本地 `server/src/db.json` 未被迁移脚本污染：
    - `solutions = 30`
    - `knowledgeSubmissions = 0`

## MinerU 图文烟雾测试
- 目标：
  - 评估 MinerU 导入链路是否能保留图文并茂效果
  - 评估 macOS 下 `kb-server.local` 是否仍需要配置
- 结果：
  - `device-shop` 样本：
    - 文件：`天翼视联-翼支付商城-赛达超清云台摄像机H681客服文档-全国20250929V2.0.docx`
    - 上传成功
    - `imageCount = 2`
    - `/api/solutions/:id/preview` 返回 `source=local`、`hasImages=true`
    - FastGPT chunk 中包含 `http://kb-server.local:3001/images/...`
  - `home-ai` 样本：
    - 文件：`天翼视联（原数字生活）-天翼看家-AI守护产品客服文档-全国-20251031V28.0.docx`
    - 上传成功
    - `imageCount = 56`
    - `/api/solutions/:id/preview` 返回 `source=local`、`hasImages=true`
    - FastGPT chunk 中包含 `http://kb-server.local:3001/images/...`
- 结论：
  - MinerU 路线明显优于纯文本导入，适合图多、表多、截图多的知识文档
  - 当前 macOS 仍需要处理 `kb-server.local` 解析，否则浏览器无法访问 FastGPT 中存储的绝对图片 URL
  - 如果仅在本机使用，最稳妥的映射是 `127.0.0.1 kb-server.local`

## macOS hosts 现状
- 当前 `/etc/hosts` 已补充：
  - `127.0.0.1 kb-server.local`
- `server/.env` 仍配置：
  - `BASE_URL=http://kb-server.local:3001`
- 已补充 macOS 脚本：
  - `update-kb-hosts-macos.sh`
- 已在本机执行完成并验证：
  - `http://kb-server.local:3001/api/solutions` 可正常访问
  - MinerU 生成的 `http://kb-server.local:3001/images/...` 图片链接返回 `200`

## 删除链路修复
- 已修正 `DELETE /api/solutions/:id` 和 `DELETE /api/capabilities/:id` 的 FastGPT collection 删除参数。
- 新增后端 helper：
  - `deleteFastGptCollections(collectionIds)`
- 实际修复点：
  - 从旧的错误 payload 改为 FastGPT 当前实例可接受的 `POST /core/dataset/collection/delete` + `{ collectionIds: [...] }`
- 已完成远端清理：
  - 旧 MinerU 烟雾测试 collection
    - `69b2aafe08bebeccb30bc79c`
    - `69b2ab6008bebeccb30bc86c`
  - 二次端到端删除回归 collection
    - `69b2ae9708bebeccb30bcd69`
- 验证结果：
  - 远端 `collection/detail?id=...` 对上述 3 个 collection 均返回 `500/unExistCollection`
  - 本地 `/api/solutions` 维持 `30` 条
  - `server/src/db.json` 中不存在上述 smoke 记录

## 端到端删除回归
- 新建临时 markdown 文档：
  - `Delete Smoke Test`
  - solution id: `1773317783824`
  - collectionId: `69b2ae9708bebeccb30bcd69`
- 通过项目接口完成完整验证：
  - `POST /api/upload` 成功创建本地 solution 和 FastGPT collection
  - `DELETE /api/solutions/1773317783824` 成功
  - 远端 FastGPT collection 同步删除
  - 本地 solution 列表恢复到 `30`

## MinerU 补传名单
- 已按 docx 内嵌图片数量整理 MinerU 重传优先级：
  - [MINERU-REIMPORT-PLAN-2026-03-12.md](/Users/tony/Documents/GitHub/kb-cs-assistant/docs/MINERU-REIMPORT-PLAN-2026-03-12.md)
- 关键结论：
  - 不需要全量重传
  - `home-ai` 中高图文文档是主补传对象
  - `baichuan` 客户端总文档与 `b2b-ict` 政企版总文档也属于高优先级
  - `device-shop` 仅 2 份文档属于“可选补传”
  - `0` 图文档和 `EBO` 文档可以继续保持纯文本导入

## MinerU 补传机制
- 已新增 MinerU helper：
  - `server/src/mineru-client.js`
- 已新增串行补传脚本：
  - `server/src/scripts/import-mineru-docx-batch.js`
- 已新增 npm 命令：
  - `npm run fastgpt:reimport-mineru-docx`
- 机制能力：
  - 串行执行
  - `mineru-reimport-state.json` 断点记录
  - `--replace-existing` 替换旧 collection
  - `--min-images` 只筛高图文文档
  - 指数退避重试
  - 文件间隔节流
- dry-run 验证通过：
  - `home-ai` + `min-images=20` + `limit=2`
  - 已正确识别 2 份文档为 `dry_run_replace`
  - 旧 collection 对应：
    - `69b289f408bebeccb30baa8b`
    - `69b289f508bebeccb30bac1c`

## MinerU 第一批实际补传
- 批次：
  - `home-ai` 第一批
  - `limit=2`
  - `min-images=20`
  - `replace-existing=true`
  - `delay-ms=8000`
- 结果目录：
  - `tmp/mineru-reimport-home-ai-batch1`
- 汇总：
  - `reimported = 2`
  - `failed = 0`
- 实际替换结果：
  - `AI守护`
    - 旧 collection：`69b289f408bebeccb30baa8b`
    - 新 collection：`69b2dd5608bebeccb30bda55`
    - `detailStatus=chunk`
    - `indexAmount=82`
  - `画面异常巡检`
    - 旧 collection：`69b289f508bebeccb30bac1c`
    - 新 collection：`69b2dd7e08bebeccb30bdddd`
    - `detailStatus=chunk`
    - `indexAmount=48`
- 验证：
  - 旧 collection detail 均返回 `unExistCollection`
  - FastGPT `data/v2/list` 中可见 `http://kb-server.local:3001/images/...`
  - 本地图片目录存在：
    - `2b3f0ac2-5574-4890-8320-67f2462b57bc` -> `56` 张
    - `6da676e8-0917-4af9-91a3-52421f1c0db4` -> `33` 张

## 下一步待办
- 后端待办：
  - 在 `/api/solutions/:id/chat` 中增加“当前文档有效引用数为 0 时强制固定兜底”的硬逻辑，不再只依赖 workflow 的 `ELSE`
  - 同时清理单文档流式输出中偶发的重复段落/重复兜底文案
- 内部支持库待办：
  - 新建内部支持知识库
  - 配置 `FASTGPT_INTERNAL_DATASET_ID`
  - 再导入 `视频AI算法舱` docx 与对应派生 markdown
- Excel Q&A CSV 待办：
  - 按目标知识库拆分后再导入
  - 不要把 `qa-fq-20260312` 或 `qa-ai-20260312` 整包灌入单一库
